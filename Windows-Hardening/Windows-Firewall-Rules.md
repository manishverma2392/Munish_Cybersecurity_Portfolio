# Windows Firewall Hardening

## Recommended Rules
- Block inbound SMB (port 445)
- Block RDP from external networks
- Allow only required inbound ports
- Enable firewall logging for dropped packets

## Why It Matters
Firewall hardening reduces attack surface and prevents lateral movement.
